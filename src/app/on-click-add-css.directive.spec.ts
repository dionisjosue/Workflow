import { OnClickAddCssDirective } from './on-click-add-css.directive';

describe('OnClickAddCssDirective', () => {
  it('should create an instance', () => 
  {

   // const directive = new OnClickAddCssDirective();
   // expect(directive).toBeTruthy();
  });
});
